version: '3'
services:
  todo-list:
    build:
      dockerfile: Dockerfile
      context: ./
    ports:
      - "81:80"
    volumes:
      - "todo-db:/etc/todos"
    networks:
      - default
volumes:
  todo-db:
